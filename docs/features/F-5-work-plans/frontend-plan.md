# План работ: F-5 Планы работ — Frontend

## 0. Контекст для агента

Перед началом прочитай в порядке:

1. `AGENTS.md` в корне репозитория.
2. [business-description.md](business-description.md): пользовательские сценарии и критерии приёмки.
3. [system-description.md](system-description.md) и [openapi.yaml](openapi.yaml): API-контракты и ограничения.
4. Реализацию F-4: страницы веток, авторизованный API-клиент, обработку ошибок и маршрутизацию.

Работай только в `frontend/src`. Все тексты интерфейса — на русском и собраны в один модуль строк по образцу F-4. Не реализуй задачи, отдельный раздел архива или восстановление — это F-6 и F-9.

## 1. Типы и API-слой

- [x] Добавить типы `WorkPlan`, `WorkPlanPage`, параметров списка и ошибок по `openapi.yaml`, а также API-методы списка, создания, чтения, переименования и архивирования.
- [x] Передавать `limit`, `cursor` и `q` через `URLSearchParams` и авторизованный клиент F-4; не передавать `userId` или `branchId` в теле создания/переименования.
- [x] Обрабатывать `400`, `401`, `404`, `502 UPSTREAM_UNAVAILABLE` и сетевые ошибки через существующие пользовательские компоненты. `401` использует refresh-механизм F-4; `502` показывает временную недоступность и повтор без refresh, очистки сессии или редиректа на вход.

**Готовность:** типы соответствуют системному описанию, API не содержит `userId`.

## 2. Список планов в ветке

- [x] Заменить заглушку страницы ветки F-4 списком активных планов, сохранив существующие переименование и архивирование ветки.
- [x] Добавить поиск по названию с очисткой запроса и отдельными состояниями «в ветке нет планов» и «по запросу ничего не найдено». Нормализовать `q` по точному правилу F-4 с пределом 150 кодовых точек и отправлять нормализованное значение.
- [x] Добавить действие «Создать план», загрузку следующей страницы по `nextCursor`, состояния загрузки и ошибки.
- [x] При изменении нормализованного `q` (включая регистр) или `branchId` сбрасывать список и курсор, отменять устаревшие запросы и не отображать ответы предыдущей ветки/поиска. При догрузке исключать дубли по `id`.
- [x] Сохранить отображение названия родительской ветки.

**Готовность:** пользователь может найти и просмотреть планы только текущей ветки; загруженные страницы не дублируют элементы.

## 3. Создание и переименование

- [x] Создать форму добавления и переименования с NFC, удалением краевых пробелов и подсчётом кодовых точек по разделу 4 F-4; проверять `name` 2–150 и отправлять нормализованное значение. Серверная валидация остаётся окончательной.
- [x] Реализовать компактную форму переименования на странице плана.
- [x] После создания сбрасывать поиск и курсор и загружать первую страницу, чтобы новый план был виден даже при более чем 50 планах. После переименования обновлять план на странице; при возврате в ветку список перечитывается с учётом текущего поиска. Серверные ошибки показывать у поля.

**Готовность:** создание и переименование соответствуют критериям 1–5 бизнес-описания.

## 4. Страница и архивирование плана

- [x] Добавить защищённый маршрут `/work-plans/:planId` с названием плана и пояснением, что задачи появятся в F-6. После `GET /api/v1/work-plans/{planId}` получать название родительской ветки существующим `GET /api/v1/branches/{branchId}` по `branchId` из ответа; если план или ветка недоступны, показывать единый экран `404`. Не расширять ответ плана полем `branchName`.
- [x] Добавить действие «Архивировать» с подтверждением: план исчезнет из рабочего списка, но сохранится в архиве.
- [x] Использовать доступный с клавиатуры диалог подтверждения F-4; повторное нажатие во время запроса не отправляет второй запрос.
- [x] После успешного архивирования вернуться на `/branches/{branchId}` из загруженного плана и показать подтверждение.
- [x] Для `404` показать «План не найден или недоступен» без технических деталей.

**Готовность:** архивированный план недоступен в активном списке и по прямому URL.

## 5. Роутинг и навигация

- [x] Добавить маршрут плана до catch-all `NotFound`.
- [x] Обновить ссылки со страницы ветки на планы.
- [x] Проверить, что существующие защищённые маршруты F-4 не меняют поведение.

**Готовность:** неавторизованный пользователь перенаправляется на `/login` без мигания интерфейса.

## 6. Финальная проверка

- [x] Сверить интерфейс с критериями приёмки бизнес-описания.
- [x] Проверить поиск, пагинацию, пустые состояния, ошибки и архивирование.
- [x] Выполнить `npm test` и `npm run build`.

**Готовность:** сборка и тесты зелёные; пользовательские сценарии F-5 выполняются.

## 7. Тестирование

Vitest и React Testing Library; API мокируется на уровне service-клиента.

| # | Сценарий | Ожидание |
| --- | --- | --- |
| FE-01 | В ветке нет планов | Показано пустое состояние и «Создать план» |
| FE-02 | Успешное создание | План появляется в списке текущей ветки |
| FE-03 | Название из 1 или 151 символа | Ошибка у поля, запрос не отправлен |
| FE-04 | Поиск | Список фильтруется, при пустом результате показано понятное сообщение |
| FE-05 | `nextCursor` | Следующая страница добавляется без дублей |
| FE-06 | Переименование | Новое название сразу видно в списке и на странице плана |
| FE-07 | Архивирование с подтверждением | План исчезает, пользователь возвращается в ветку |
| FE-08 | `404` плана | Экран «План не найден или недоступен» |
| FE-09 | Неавторизованный переход | Редирект на `/login` |
| FE-10 | `name` и `q` с emoji, составным символом, краевыми U+00A0/U+2003 и границами 150/151 кодовая точка | Клиент применяет правило F-4; 151 точка не отправляется |
| FE-11 | Смена поиска или ветки при незавершённой загрузке, старый ответ пришёл позже | На экране только данные последнего `branchId` и `q`; курсор сброшен |
| FE-12 | Создать план при активном поиске и более чем 50 планах | Поиск сброшен, загружена первая страница, новый план виден |
| FE-13 | Переименовать план так, что он не совпадает с текущим поиском | Карточка удалена из отфильтрованного списка без устаревшего результата |
| FE-14 | Открыть план и загрузить родительскую ветку; ветка вернула `404` | Имя ветки получено через API F-4; при `404` показан экран недоступного плана |
| FE-15 | Gateway вернул `502 UPSTREAM_UNAVAILABLE` | Показана временная недоступность и повтор; refresh не вызывается, сессия не очищается |
| FE-16 | Архивирование с клавиатуры и повторное нажатие подтверждения | Фокус управляется корректно, вторая операция не отправляется |
