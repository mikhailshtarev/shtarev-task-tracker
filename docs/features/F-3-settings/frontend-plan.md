# План работ: F-3 Персональные настройки и профиль — Frontend

## 0. Контекст для агента

Прежде чем начать, прочитай в этом порядке:

1. `AGENTS.md` в корне репозитория — общие правила + раздел «Структура документации фич».
2. [business-description.md](business-description.md) — бизнес-правила, диапазоны, критерии приёмки.
3. [system-description.md](system-description.md) — API-контракты (раздел 4), маршруты (раздел 7).
4. `docs/technical/auth/frontend-auth-integration.md` — существующая инфраструктура: `api/client.ts`, `AuthContext`, `ProtectedRoute`, `ErrorDisplay`, роутинг.

**Правила ведения плана:**

- Выполняй этапы строго по порядку; отмечай выполненные пункты (`- [x]`) — это твой якорь контекста.
- Изменения — в `frontend/src/` и `frontend/package.json`; существующие компоненты авторизации (F-2) переиспользуем, не переписываем.
- Стили — в рамках текущей дизайн-системы (`src/styles/`), компоненты — небольшие, одна ответственность.
- Все тексты интерфейса — на русском; ошибки — через существующий `ErrorDisplay`.

## 1. API-слой и типы

- [x] Типы `UserProfile`, `UserSettings` по контрактам раздела 4 системного описания.
- [x] В `authService.ts` добавить обёртки: `updateProfile(name)` → `PUT /auth/me`; `getSettings()` → `GET /auth/settings`; `updateSettings(settings)` → `PUT /auth/settings` (через существующий `request` с `credentials` и Bearer).
- [x] Валидаторы в `utils/validators.ts`: имя (0–100), длительность помидора (целое 5–120, кратное 5), `budgetHourCost` (целое 1–1000), `estimationUnit` ∈ {hours, pomodoros}; сообщения на русском.

**Готовность:** `npm run build` проходит, типы экспортированы.

## 2. Экран профиля `/profile`

- [x] Компонент `ProfilePage.tsx`: email (read-only), поле имени с кнопкой «Сохранить», ссылка на `/change-password` (F-2).
- [x] Сабмит: клиентская валидация → `updateProfile` → при 200 обновить `user` в `AuthContext` (имя сразу видно в шапке/меню), показать подтверждение; при 400 — `details` под полем; при 401 — штатная обработка interceptor'а.
- [x] Пустое имя (`null`): поле пустое, сохранение пустого значения допустимо (очистка имени).

**Готовность:** изменение имени переживает перезагрузку страницы (данные из `GET /auth/me`).

## 3. Экран настроек `/settings`

- [x] Компонент `SettingsPage.tsx`, секции по бизнес-описанию:
  - **Оценка**: переключатель «Часы / Помидоры»; поле «Длительность помидора (мин)» — видно только при выбранной единице «Помидоры»;
  - **Игровой режим**: переключатель вкл/выкл; поле «Стоимость часа для бюджета (z)»;
  - кнопка «Сохранить» — полное обновление (`PUT` со всеми полями).
- [x] Загрузка текущих значений через `getSettings()` при монтировании; состояние блокируется на время загрузки.
- [x] Сабмит: клиентская валидация → `updateSettings` → при 200 подтверждение и обновление локального состояния; при 400 — `details` под соответствующими полями; при 401 — стандартная обработка.
- [x] Подсказки под полями: диапазоны значений (5–120 мин с шагом 5, 1–1000) — понятным языком, без технических деталей.
- [x] Визуальное скрытие игровых элементов интерфейса при выключенном режиме — **не в этой фиче** (F-13); здесь только сохранение флага.

**Готовность:** критерии приёмки 2–4, 6 бизнес-описания воспроизводятся вручную (округление 1,5 ч → 4 помидора проверяется на этапе 8 юнит-тестом конвертации).

## 4. Роутинг и навигация

- [x] Маршруты `/profile` и `/settings` за `ProtectedRoute`; `/*` → NotFound остаётся последним.
- [x] Пункты «Профиль» и «Настройки» в правом верхнем углу шапки авторизованной части.
- [x] Обновить таблицу роутинга в `docs/technical/auth/frontend-auth-integration.md` (раздел 6): добавить две строки.

**Готовность:** доступ к `/profile` и `/settings` гостем → редирект на `/login` без мигания (через `isAuthLoading`).

## 5. Конвертация единиц

- [x] Утилита `utils/unitConversion.ts`: `hoursToDisplay(hours, unit, pomodoroMinutes)` — часы → часы (с точностью 5 минут) или целые помидоры (округление 0,5 вверх по правилам отображения из БТ 4.6); `displayToHours(value, unit, pomodoroMinutes)` для ввода. Ввод и сохранение времени приводятся к шагу 5 минут.
- [x] Юнит-тесты на утилиту (этап 7) — эта логика будет переиспользована F-6 (карточки задач) и F-8 (доска).

**Готовность:** кейс приёмки 3 («1,5 часа → 4 помидора при 25 мин») покрыт тестом.

## 6. Финальная сборка

- [ ] Сквозная проверка критериев приёмки 1–7 из бизнес-описания на dev-стенде (бэкенд F-3).
- [x] Мелкая ревизия: нигде не храним настройки в `localStorage` — источник истины сервер, локально только состояние формы.

**Готовность:** `npm run build` и `npm test` зелёные; smoke-сценарий «вход → профиль (сменить имя) → настройки (сменить единицу) → перезагрузка → всё сохранилось» проходит.

## 7. Тестирование

Vitest + React Testing Library (инфраструктура с F-2). API мокируется на уровне `authService`/`fetch` — тесты не требуют работающего бэкенда. Тест-кейсы:

**Юнит-тесты:**

| # | Сценарий | Ожидание |
| --- | --- | --- |
| FE-01 | Валидаторы настроек: длительность помидора 5–120 с шагом 5; `z` 1–1000; нецелые числа | Ошибки вне диапазона или не на шаге 5, успех на границах |
| FE-02 | Валидатор имени: 0–100 символов, 101 — ошибка | Соответствует серверной валидации |
| FE-03 | `unitConversion`: 1,5 ч при 25 мин → 4 помидора; 2 ч → 4,8... 5 помидоров при 25 мин (2/0,4167); округление только отображения | Кейс приёмки 3 и обратная конвертация без потери точности |

**Компонентные тесты:**

| # | Сценарий | Ожидание |
| --- | --- | --- |
| FE-04 | `ProfilePage`: сохранение имени → `PUT /auth/me`, `user` в контексте обновлён, имя видно в шапке | Мок `authService` |
| FE-05 | `ProfilePage`: 400 `VALIDATION_ERROR` → detail под полем имени | Через `ErrorDisplay` |
| FE-06 | `SettingsPage`: монтирование → `GET /auth/settings`, форма заполнена дефолтами/текущими | Мок `authService` |
| FE-07 | `SettingsPage`: переключение на «Помидоры» показывает поле длительности; «Часы» — скрывает | Условное поле |
| FE-08 | `SettingsPage`: сабмит вне диапазона → клиентская ошибка, запрос не отправлен | Валидация до сети |
| FE-09 | `SettingsPage`: успешный сабмит → `PUT /auth/settings` со всеми полями, подтверждение | Полное обновление |
| FE-10 | `SettingsPage`: 400 с `details` по нескольким полям → ошибки под каждым полем | Маппинг details |
| FE-11 | Роутинг: гость на `/profile` и `/settings` → редирект на `/login`; при `isAuthLoading` редиректа нет | `ProtectedRoute` |
| FE-12 | `SettingsPage`: сбой `GET` (сеть) → понятное сообщение, форма не в невалидном состоянии | Обработка ошибки загрузки |

**Готовность:** `npm test` — все кейсы зелёные; `npm run build` без ошибок.
